---
import cn from "classnames";
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a"> {
  size?: "sm" | "md" | "lg";
}

const { href, size = "md", ...props } = Astro.props;
---

<a
  class={cn("hover:underline underline-offset-2", {
    "text-md": size === "md",
    "text-sm": size === "sm",
    "text-lg": size === "lg",
  })}
  {...props}
>
  <slot />
</a>
